// Code generated by Kucoin Universal SDK Generator; DO NOT EDIT.

import { instanceToPlain, Exclude, plainToInstance } from 'class-transformer';
import { WsMessage } from '@model/common';
import { WebSocketMessageCallback } from '@internal/interfaces/websocket';
import { Response } from '@internal/interfaces/response';
export class IndexPriceEvent implements Response<IndexPriceEvent, WsMessage> {
    /**
     *
     */
    symbol?: string;
    /**
     *
     */
    granularity?: number;
    /**
     *
     */
    timestamp?: number;
    /**
     *
     */
    value?: number;
    /**
     * common response
     */
    @Exclude()
    private commonResponse?: WsMessage;

    setCommonResponse(response: WsMessage): void {
        this.commonResponse = response;
    }

    fromJson(input: string): IndexPriceEvent {
        const jsonObject = JSON.parse(input);
        return plainToInstance(IndexPriceEvent, jsonObject);
    }

    toJson(): string {
        return JSON.stringify(instanceToPlain(this));
    }
    fromObject(jsonObject: Object): IndexPriceEvent {
        return plainToInstance(IndexPriceEvent, jsonObject);
    }
}

export type IndexPriceEventCallback = (
    topic: string,
    subject: string,
    data: IndexPriceEvent,
) => void;

export class IndexPriceEventCallbackWrapper implements WebSocketMessageCallback {
    callback: IndexPriceEventCallback;

    constructor(callback: IndexPriceEventCallback) {
        this.callback = callback;
    }

    onMessage(msg: WsMessage): void {
        let event = new IndexPriceEvent().fromObject(msg.rawData);
        event.setCommonResponse(msg);
        this.callback(msg.topic, msg.subject, event);
    }
}
