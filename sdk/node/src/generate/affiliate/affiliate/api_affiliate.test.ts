import { GetAccountResp } from './model_get_account_resp';
import { RestResponse } from '@model/common';

describe('Auto Test', () => {
    test('getAccount request test', () => {
        /**
         * getAccount
         * Get Account
         * /api/v2/affiliate/inviter/statistics
         */
    });

    test('getAccount response test', () => {
        /**
         * getAccount
         * Get Account
         * /api/v2/affiliate/inviter/statistics
         */
    });
});
