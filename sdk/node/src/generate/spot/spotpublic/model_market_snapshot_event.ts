// Code generated by Kucoin Universal SDK Generator; DO NOT EDIT.

import { Type, instanceToPlain, Exclude, plainToClassFromExist } from 'class-transformer';
import { MarketSnapshotData } from './model_market_snapshot_data';
import { WsMessage } from '@model/common';
import { WebSocketMessageCallback } from '@internal/interfaces/websocket';
import { Response } from '@internal/interfaces/serializable';

export class MarketSnapshotEvent implements Response<WsMessage> {
    /**
     *
     */
    sequence: string;
    /**
     *
     */
    data: MarketSnapshotData;

    private constructor() {
        // @ts-ignore
        this.sequence = null;
        // @ts-ignore
        this.data = null;
    }
    /**
     * common response
     */
    @Exclude()
    private commonResponse?: WsMessage;

    setCommonResponse(response: WsMessage): void {
        this.commonResponse = response;
    }

    toJson(): string {
        return JSON.stringify(instanceToPlain(this));
    }

    static fromJson(input: string): MarketSnapshotEvent {
        return this.fromObject(JSON.parse(input));
    }

    static fromObject(jsonObject: Object): MarketSnapshotEvent {
        return plainToClassFromExist(new MarketSnapshotEvent(), jsonObject);
    }
}

export type MarketSnapshotEventCallback = (
    topic: string,
    subject: string,
    data: MarketSnapshotEvent,
) => void;

export class MarketSnapshotEventCallbackWrapper implements WebSocketMessageCallback {
    constructor(private callback: MarketSnapshotEventCallback) {
        this.callback = callback;
    }

    onMessage(msg: WsMessage): void {
        let event = MarketSnapshotEvent.fromObject(msg.data);
        event.setCommonResponse(msg);
        this.callback(msg.topic, msg.subject, event);
    }
}
